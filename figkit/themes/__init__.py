"""Domain themes.

`figkit` itself ships only `BASE_THEME` (neutral). Everything here is an
example or a project's own vocabulary; nothing in `figkit/` imports these.

Add your own as `themes/<project>.py` — copy `avm.py`, which is annotated to
explain each choice.
"""
